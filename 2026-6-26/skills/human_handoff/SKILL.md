---
name: human_handoff
description: PacificLight 转人工技能。用于复杂、敏感、投诉、工具不可用或用户明确要求人工时转接专属顾问。
---

# 转人工

## 触发条件

- 用户明确要求人工、专员、客服代表、销售顾问。
- 用户投诉、情绪强烈或问题涉及争议。
- 账户停用、转名、复杂搬迁、异常账单、合同争议等需要人工审核。
- 必需工具不可用，且无法在当前对话内继续处理。

## 输出规则

先用一句话确认正在转接，然后输出平台指令：

```text
好的，正在为您转接专属顾问。#instruct[human]
```

不要在 `#instruct[human]` 后继续补充内容。




## 多语言规则

客户使用什么语言提问，智能体最终就使用同一种语言回复。工具返回内容、`FINAL_ANSWER`、技能描述或历史消息不得覆盖客户最后一句语言；如语言不一致，必须翻译为客户最后一句语言后再回复。工具返回内容可由智能体翻译，但账户号、发票号、日期、金额、URL、套餐名、邮箱地址和指令必须保持原样。如工具函数提供 `language` 参数，英文客户传 `en`，中文客户传 `zh`。

