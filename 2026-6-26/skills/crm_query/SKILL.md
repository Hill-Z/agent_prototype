---
name: crm_query
description: PacificLight CRM 查询技能。用于查询客户档案、合同状态、当前计划、服务地址和销售负责人。
---

# CRM 查询

用于查询客户基本档案、合同与销售负责人信息。

## 可用工具

### query_crm(account_no, customer_id, phone, email)

根据账户号、客户 ID、手机号或邮箱查询客户资料。

## 使用规则

1. 涉及服务地址、合同、销售负责人等信息时，应先完成身份验证。
2. 不要猜测客户资料；以工具返回为准。
3. 未查到客户时，要求用户核对账户号、邮箱或手机号。




## 多语言规则

客户使用什么语言提问，智能体最终就使用同一种语言回复。工具返回内容、`FINAL_ANSWER`、技能描述或历史消息不得覆盖客户最后一句语言；如语言不一致，必须翻译为客户最后一句语言后再回复。工具返回内容可由智能体翻译，但账户号、发票号、日期、金额、URL、套餐名、邮箱地址和指令必须保持原样。如工具函数提供 `language` 参数，英文客户传 `en`，中文客户传 `zh`。

