WhatsApp plain-text conversion verification

Result: pass

Checks performed:
- Python compile passed for all skills/*/scripts/tool.py files.
- Key customer-visible FINAL_ANSWER smoke tests passed without ##, ###, **, Markdown links, or HTML tags:
  - residential billing query
  - invoice/PDF link
  - commercial quote
  - residential fixed-plan recommendation
  - commercial consumption report request
  - Additional Recipient Ticket review
  - move-house dates with year validation (rejects stale and missing years)
  - move-house ticket response
- All packages/skills/*.zip files rebuilt and verified as valid UTF-8 zips containing SKILL.md and scripts/tool.py.

Notes:
- Chat outputs are plain text for WhatsApp.
- Email body and ticket content can still use HTML rich text because they are not WhatsApp chat bubbles.
- Additional Recipient Ticket output is plain text and contains no obsolete recipient-ticket wording.
- Move-house tool accepts separate `move_out_date` and `new_supply_date` values, with a strict legacy fallback for two explicit dates.
